# 01 - Flink 是什么？

## 一句话理解

**Apache Flink 是一个用于处理实时数据流和批量数据的分布式计算框架。**

它最核心的能力是：**持续不断地处理正在产生的数据，并且可以做到低延迟、高吞吐、可容错、支持状态计算。**

简单理解：

> 如果说数据库主要负责“存储数据”，那么 Flink 更擅长“在数据流动过程中实时计算数据”。

---

## Flink 主要解决什么问题？

在很多业务系统里，数据不是一次性产生的，而是源源不断地产生的，例如：

- 用户点击、浏览、下单行为
- 订单支付状态变化
- 日志采集
- IoT 设备上报数据
- 风控事件
- 实时监控指标
- 消息队列中的业务事件

这些数据通常会进入 Kafka、Pulsar 等消息系统，然后需要被实时消费、实时计算、实时落库或实时告警。

Flink 就是用来处理这类场景的。

---

## Flink 的核心定位

Flink 可以理解为一个：

> **面向有状态流处理的分布式计算引擎。**

这里有几个关键词：

### 1. 流处理

流处理指的是数据一条一条、持续不断地到来，程序也持续不断地计算。

例如：

```text
用户点击 -> Kafka -> Flink 实时计算 -> 实时大屏 / 数据库 / 告警系统
```

Flink 可以在数据刚产生后很短时间内完成计算，因此适合实时业务。

### 2. 批处理

虽然 Flink 以流处理能力著名，但它也支持批处理。

Flink 的理念是：

> 批数据可以看成是“有边界的数据流”，流数据是“无边界的数据流”。

也就是说，Flink 底层更偏向用统一的流式思想来处理流和批。

### 3. 分布式

Flink 可以运行在多台机器上，把一个大的计算任务拆分成多个并行任务执行。

这样可以提升处理能力，适合大规模数据场景。

### 4. 有状态计算

很多实时计算并不是简单地来一条处理一条，而是需要记住历史信息。

例如：

- 统计最近 5 分钟每个商品的点击量
- 判断一个用户 10 分钟内是否连续登录失败 5 次
- 计算每个用户的累计消费金额
- 对订单事件和支付事件进行匹配

这些历史信息就叫 **状态（State）**。

Flink 对状态计算支持非常强，这是它区别于普通消息消费程序的重要能力。

---

## Flink 能做哪些事情？

常见使用场景包括：

### 1. 实时统计

例如：

- 实时 UV / PV
- 实时订单金额
- 实时销售大盘
- 实时用户行为分析

### 2. 实时风控

例如：

- 异常登录检测
- 高频交易检测
- 羊毛党识别
- 支付风险识别

### 3. 实时数仓

Flink 经常用于实时数仓链路中：

```text
业务系统日志 / Binlog / Kafka
        ↓
      Flink
        ↓
实时明细层 / 汇总层 / OLAP / 数据大屏
```

### 4. 实时 ETL

ETL 指数据抽取、转换、加载。

Flink 可以从 Kafka、数据库、文件系统等数据源读取数据，清洗转换后写入下游系统。

### 5. 复杂事件处理

例如识别一组有先后顺序的事件：

```text
浏览商品 -> 加入购物车 -> 下单 -> 支付
```

如果某些步骤异常或超时，就可以触发告警或补偿逻辑。

---

## Flink 和普通 Java 程序有什么区别？

普通 Java 程序当然也可以消费 Kafka 并处理数据，但如果业务复杂后，会遇到很多问题：

- 怎么保证程序挂了以后能从上次位置继续处理？
- 怎么保存每个用户、每个商品、每个订单的中间状态？
- 怎么处理乱序数据？
- 怎么做窗口统计？
- 怎么保证数据不重复、不丢失？
- 怎么把计算任务分布到多台机器上？
- 怎么动态扩容？

Flink 把这些通用能力封装成了框架能力，开发者只需要重点关注业务计算逻辑。

---

## Flink 的几个重要特性

### 1. 低延迟

Flink 适合毫秒级到秒级的实时计算场景。

### 2. 高吞吐

Flink 可以通过并行计算处理大量数据。

### 3. 容错机制

Flink 支持 Checkpoint 机制。

当任务失败后，可以从最近一次 Checkpoint 恢复，尽量保证数据处理的正确性。

### 4. 状态管理

Flink 可以管理大规模状态数据，例如每个用户、每个订单、每个设备的历史状态。

### 5. 事件时间处理

Flink 支持按照事件真正发生的时间进行计算，而不是只按照数据到达 Flink 的时间计算。

这对于处理网络延迟、消息乱序非常重要。

---

## 一个简单例子

假设我们要统计每 1 分钟内每个商品的点击次数。

数据流可能是这样的：

```text
用户A 点击 商品1
用户B 点击 商品2
用户C 点击 商品1
用户D 点击 商品1
```

Flink 可以实时消费这些点击事件，然后按商品分组，再按照 1 分钟窗口聚合，最终输出：

```text
商品1：3 次点击
商品2：1 次点击
```

这类计算在电商实时大屏、推荐系统、广告系统中非常常见。

---

## 学习 Flink 时需要重点掌握什么？

后续学习可以按照下面顺序推进：

1. Flink 是什么、适合什么场景
2. Flink 基础架构：JobManager、TaskManager、Slot
3. DataStream API
4. Source、Transformation、Sink
5. Time、Window、Watermark
6. State 状态管理
7. Checkpoint 与容错机制
8. Flink SQL
9. Kafka 与 Flink 集成
10. 实时数仓与实际案例

---

## 本节总结

Flink 是一个用于实时数据处理的分布式计算框架。

它最大的价值在于：

- 能处理源源不断到来的数据流
- 能进行复杂的有状态计算
- 支持窗口、时间、乱序数据处理
- 支持故障恢复
- 可以运行在分布式环境中处理大规模数据

一句话总结：

> Flink 是实时数据处理领域非常重要的计算引擎，适合用来构建实时统计、实时风控、实时数仓、实时 ETL 等系统。
